services:
  frontend:
    build:
      context: ./front
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    restart: unless-stopped
    depends_on:
      - backend

  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: appuser
      POSTGRES_PASSWORD: appsecret
      POSTGRES_DB: appdb
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./backend/db/init.sql:/docker-entrypoint-initdb.d/init.sql
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: appuser
      DB_PASSWORD: appsecret
      DB_NAME: appdb
      JWT_SECRET: super-secret-jwt-key-change-in-productions
      JWT_REFRESH_SECRET: super-secret-refresh-key-change-in-production
      NODE_ENV: production
      COOKIE_DOMAIN: ""
      CSRF_ORIGIN: ""
      ALLOWED_ORIGINS: ""
    volumes:
      - uploads:/app/uploads
    depends_on:
      - postgres
    restart: unless-stopped

volumes:
  pgdata:
  uploads:
