# Secrets — NEVER commit
.env
.env.*
!.env.example

# Node
node_modules
dist
dist-ssr

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode
.idea
.DS_Store
*.swp

# Runtime uploads are persistent local data, not source files
storage/uploads/

# Local Kubernetes secrets
k8s/secret.yaml

# Local database backups may contain credentials and session data
*_backup.sql
*_backup.sql.gz
